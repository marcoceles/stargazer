# stargazers
> Get list of stargazers of a repository from Github's API.

## Installation

Open Terminal;

Run:

```shell
git clone https://github.com/marcoceles/stargazer.git
```

## Install Pods

Go to the project folder and run:

```shell
 pod install
```
